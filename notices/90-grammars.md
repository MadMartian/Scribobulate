# Syntax and Theme Grammars

Scribobulate bundles syntax-highlighting grammar definitions via the `two-face` crate (which packages the syntax assets used by `bat`). The crate is dual-licensed MIT OR Apache-2.0; the embedded grammar definitions carry their own upstream licenses, reproduced below as required by their terms. Generated from `two_face::acknowledgement::listing()` (two-face v0.5.1).

**Licenses present:** MIT, Apache-2.0, BSD-2-Clause (incl. the FreeBSD variant), BSD-3-Clause — all OSI-approved permissive licenses that require attribution; none are copyleft or source-available. The grammar definitions are bundled **unmodified**. Scribobulate itself is licensed Apache-2.0 (see `LICENSE`).

