# Remote image fixture

A remote image reference over HTTPS (used by §14.1/§14.2 — with the
Show-Unsafe-Images toggle OFF it must show a broken-image placeholder; ON it
fetches over the network):

![a remote raster](https://picsum.photos/id/237/240/180.jpg)

Body text after the image so the paragraph flow is visible.
