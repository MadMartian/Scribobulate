# Line-break rendering

These three lines are separated by a single newline each and must render on
their own lines (hard-wrap model):

Line one
Line two
Line three

The paragraph above ended with a blank line, so this is a separate, gap-separated
paragraph. This sentence itself is
wrapped across two source lines and must show as two rendered lines too.
