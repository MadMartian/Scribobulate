# Fixture for check 12. BOM + non-ASCII = legal, and deliberately so: the invariant
# is a DISJUNCTION. This file must PASS, or the check is banning the legitimate case
# instead of the dangerous one.
$msg = "an em dash — inside a literal"
