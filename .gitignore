/target
# Windows packaging output: the staged runtime tree and the compiled installer,
# both regenerated by packaging/windows/stage.ps1 + scribobulate.iss.
/build
# GENERATED by build.rs from notices/*.md + two_face::acknowledgement::listing().
# Deliberately not versioned: a committed copy of a derived artefact went stale for the
# life of the project while claiming to be generated. The authored INPUTS in notices/
# are versioned; this output is rebuilt on every cargo build.
/THIRD-PARTY-LICENSES.md
/git.commit.txt
/.claude/settings.local.json
.agents/skills
.claude/skills
.idea
*.log
tests/reports
docs
.claude/agents/

# macOS Finder metadata (created by any Finder visit to the tree)
.DS_Store
git.commit.txt
